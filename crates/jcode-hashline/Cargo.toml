[package]
name = "jcode-hashline"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
similar = "2"           # line diff for stale-tag recovery (already a workspace dep)

[dev-dependencies]
tempfile = "3"
